func main(){
    for i in range(1,100){
        var buffer = "";
        var met_handle = false;
        
        if i % 3 == 0{
            buffer += "Fizz";
        }
        if i % 5 == 0{
            buffer += "Buzz";
        }
        if buffer.len() == 0 {
            println(i);
        } else {
            println(buffer);
        }
    }
}
main()